/*const Router = require('express');
const router = new Router();
const product_reviewController = require('../controllers/product_reviewController');

router.post('/', product_reviewController.create);
router.get('/', product_reviewController.getAll);
router.get('/:product_id', product_reviewController.getOne);

module.exports = router;*/