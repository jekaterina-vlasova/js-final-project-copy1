/*const Router = require('express');
const router = new Router();
const cart_productController = require('../controllers/cart_productController');

router.post('/', cart_productController.create);
router.get('/:id', cart_productController.getOne);

module.exports = router;*/